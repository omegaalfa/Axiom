[package]
name = "axiom-editor"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Core do editor de texto do Axiom — buffer, cursor, seleção, histórico"

[dependencies]
# Editor core headless publicado no crates.io (v0.2.0, 15 July 2026)
# Componente isolado sem dependências de UI Floem/wgpu/winit.
# License: MIT
floem-editor-core = "0.2.0"
tempfile = "3.27"
